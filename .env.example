# Copy to .env for local overrides. .env is git-ignored; never commit real secrets.

# Runtime environment name: development | production
PITCH_ENV=development

# Host/port for the API when run behind a process manager (informational for now).
# PITCH_API_HOST=127.0.0.1
# PITCH_API_PORT=8000
